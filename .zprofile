## sets the environment for login shells, it contains "set it and forget it" type of parameters
## such as update $PATH

source ~/.cprofile

# >>> oh my posh >>>
eval "$(oh-my-posh init zsh --config '~/.ohmyposh/amro-custom.json')"
# <<< oh my posh <<<